{{#each (array 0 1 2) as |layerIndex|}}
  {{silos-layer
      layerIndex=layerIndex
      numSilos=5
      hasTabBar=(eq layerIndex 0)
  }}
{{/each}}

{{to-elsewhere
    named="layer-0-silo-0"
    send=(component 'more-contents')
}}

{{outlet}}
