{{to-nav-stack
    layer=layerIndex
    item=(component
            routeComponent
            model=model
            layerIndex=layerIndex
            siloIndex=siloIndex
            rootPage=rootPage
            drillDownToRatingForm=(route-action 'drillDownToRatingForm')
            back=(route-action 'back')
            visitMySchedule=(route-action 'visitMySchedule')
    )
    titleBar=(component titleBarComponent
      model=model
      siloIndex=siloIndex
      rootPage=rootPage
      back=(route-action 'back')
      visitMySchedule=(route-action 'visitMySchedule')
    )
}}

{{outlet}}
