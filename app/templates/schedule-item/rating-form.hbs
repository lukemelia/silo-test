{{to-nav-stack
    layer=layerIndex
    item=(component
            routeComponent
            model=model
            layerIndex=layerIndex
            siloIndex=siloIndex
            rootPage=rootPage
            back=(route-action 'back')
     )
     titleBar=(component titleBarComponent
       model=model
       siloIndex=siloIndex
       rootPage=rootPage
       back=(route-action 'back')
     )
}}

{{outlet}}
