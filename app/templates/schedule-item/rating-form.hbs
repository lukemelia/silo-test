{{to-nav-stack
    layer=layerIndex
    item=(component
            routeComponent
            model=model
            layerIndex=layerIndex
            back=(route-action 'back')
     )
     titleBar=(component titleBarComponent
       model=model
       back=(route-action 'back')
     )
}}

{{outlet}}
