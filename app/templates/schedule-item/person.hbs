{{to-nav-stack
    layer=layerIndex
    item=(component
            routeComponent
            model=model
            layerIndex=layerIndex
            siloIndex=siloIndex
            rootPage=rootPage
     )
     titleBar=(component titleBarComponent
       model=model
       siloIndex=siloIndex
       rootPage=rootPage
       close=(route-action 'close')
     )
}}

{{outlet}}
