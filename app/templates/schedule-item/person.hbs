{{to-nav-stack
    layer=layerIndex
    item=(component
            routeComponent
            model=model
            layerIndex=layerIndex
     )
     titleBar=(component titleBarComponent
       model=model
       close=(route-action 'close')
     )
}}

{{outlet}}
