{{#if shouldRenderMore}}
  {{to-nav-stack
      layer=0
      item=(component 'routable-components/yapp/more')
      titleBar=(component 'routable-components/yapp/more/title-bar')
  }}
{{/if}}

{{to-nav-stack
    layer=layerIndex
    item=(component routeComponent model=model)
    titleBar=(component titleBarComponent model=model)
}}

{{outlet}}
